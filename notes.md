# Addresses appear:<br>

Accounts:<br>
 * on accoung login:
 * new account?
	
Txns:<br>
 * When creating transactons

Commands:<br>

check balance:<br>
 * of sender (db lookup)

transaction:<br>
 * sender (db lookup)
 * first argument (db lookup)